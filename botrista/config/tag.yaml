# config file from github

apriltag:                 # node name
  ros__parameters:
    # setup (defaults)
<<<<<<< HEAD
    image_transport: "compressed"  # image format: "raw" or "compressed"
=======
    image_transport: compressed  # image format: "raw" or "compressed"
>>>>>>> 460e438 (Added localizer node that localizes camera to the robot)
    family: 36h11         # tag family name: 16h5, 25h9, 36h11
    size: 0.080           # default tag edge size in meter
    profile: false        # print profiling information to stdout

    # tuning of detection (defaults)
    max_hamming: 0        # maximum allowed hamming distance (corrected bits)
    detector:
<<<<<<< HEAD
      threads: 4         # number of threads
      decimate: 1.0      # decimate resolution for quad detection
      blur: 0.2         # sigma of Gaussian blur for quad detection
=======
      threads: 2          # number of threads
      decimate: 2.0       # decimate resolution for quad detection
      blur: 0.0           # sigma of Gaussian blur for quad detection
>>>>>>> 460e438 (Added localizer node that localizes camera to the robot)
      refine: 1           # snap to strong gradients
      sharpening: 0.25    # sharpening of decoded images
      debug: 0            # write additional debugging images to current working directory

    # (optional) list of tags
    # If defined, 'frames' and 'sizes' must have the same length as 'ids'.
    tag:
<<<<<<< HEAD
      ids:    [0, 1, 2, 3]         # tag IDs for which to publish transform
      frames: ['camera_localizer_tag', 'kettle_tag', 'filter_tag', 'pour_over_tag']           # frame names
      sizes:  [0.140, 0.080, 0.080, 0.080]             # tag-specific edge size, overrides the default 'size'
=======
      ids:    [0]         # tag IDs for which to publish transform
      frames: ['camera_localizer_tag']           # frame names
      sizes:  [0.080]             # tag-specific edge size, overrides the default 'size'
>>>>>>> 460e438 (Added localizer node that localizes camera to the robot)
